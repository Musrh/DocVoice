{
  "name": "vite-vue-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "engines": {
    "node": ">=20"
  },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "deploy": "npm run build"
  },

  "dependencies": {
    "vue": "^3.5.32",
    "vue-router": "^4",
    "tesseract.js": "^5.0.0"
  },

  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.4"
  }
}
